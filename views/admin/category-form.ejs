<%- include('../partials/header') %>
<%- include('../partials/navbar') %>

<div class="container">
    <main class="content">
        <div class="admin-header" style="margin-bottom: 25px;">
            <h2>Thêm danh mục mới</h2>
        </div>

        <div class="recipe-form">
            <form action="/admin/categories/create" method="POST">
                <div class="form-group">
                    <label style="font-weight: 600; color: #475569; margin-bottom: 6px;">Tên danh mục</label>
                    <input type="text" name="name" placeholder="Nhập tên danh mục..." required>
                </div>

                <div class="form-group" style="margin-top: 15px;">
                    <label style="font-weight: 600; color: #475569; margin-bottom: 6px;">Mô tả danh mục</label>
                    <textarea name="description" rows="4" placeholder="Nhập mô tả danh mục..."></textarea>
                </div>

                <div style="display: flex; gap: 12px; margin-top: 25px;">
                    <button type="submit" class="btn-submit" style="background: #F28944; border: none; padding: 10px 24px; border-radius: 20px; font-weight: 600; color: #fff; cursor: pointer;">
                        Lưu danh mục
                    </button>
                    <a href="/admin/categories" class="btn-clear-search" style="text-decoration: none; padding: 10px 24px; border-radius: 20px; font-weight: 600; display: inline-block;">
                        Hủy bỏ
                    </a>
                </div>
            </form>
        </div>
    </main>

    <% const activeTab = 'categories'; %>
    <%- include('../partials/admin-sidebar', { activeTab }) %>
</div>

<%- include('../partials/footer') %>